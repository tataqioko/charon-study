# 本地开发环境变量（此文件不会提交到仓库）
# 如需自定义 API 配置，请复制此文件为 .env.local 并修改

# 自定义 API 站点（仅开发环境）
VITE_DEV_API_BASE_URL=https://api.nktp.top/v1

# 自定义 API Key（仅开发环境，可选）
# VITE_DEV_API_KEY=your-key-here

# 自定义默认模型（仅开发环境，可选）
# VITE_DEV_DEFAULT_MODEL=gpt-4
